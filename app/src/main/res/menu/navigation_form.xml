<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item android:id="@+id/info"
        android:icon="@android:drawable/ic_menu_share"
        android:title="share"
        app:showAsAction="always"/>

    <item android:id="@+id/searchicon"
        android:icon="@android:drawable/ic_menu_search"
        android:title="search"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView" />
    <group >
        <item
            android:id="@+id/expandablelist"
            android:icon="@drawable/ic_menu_send"
            android:title="Expandable List" />
        <item
            android:id="@+id/filterlist"
            android:icon="@drawable/ic_menu_send"
            android:title="Filter List" />
        <item
            android:id="@+id/spinnersearch"
            android:icon="@drawable/ic_menu_send"
            android:title="Spinner Search" />
    </group>

    <group >
        <item
            android:id="@+id/alertdialog"
            android:icon="@drawable/ic_menu_send"
            android:title="Alert Dialog" />
        <item
            android:id="@+id/tableview"
            android:icon="@drawable/ic_menu_send"
            android:title="Table View" />
        <item
            android:id="@+id/dynamicviews"
            android:icon="@drawable/ic_menu_send"
            android:title="Dynamic Views" />
    </group>
</menu>
